# Theme-import fixture

This palette tests data-only theme imports, persistence, validation, and
editor/terminal state preservation. It is not a built-in theme or a published
theme package. The original license is retained alongside it.

The retired standalone package is recoverable from Git tag `v1.0.8`.
